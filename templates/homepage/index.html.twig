{% extends "base.html.twig" %}

{% block body %}
    <h1>Accueil</h1>
    <p>
        <a href="{{ url('homepage.index') }}">Retour à l'accueil</a>
    </p>
    <p>
        <a href="{{ url('homepage.index', {name: 'Allan'}) }}">Bonjour</a>
    </p>
{% endblock %}